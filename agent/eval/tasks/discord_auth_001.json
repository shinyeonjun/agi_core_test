{
  "id": "discord_auth_001",
  "suite": "discord",
  "description": "Discord routing primitives enforce allowlist, mention stripping, and chunk limits",
  "steps": [
    {
      "cmd": "agentctl self-check bridge"
    }
  ],
  "expect": {
    "ok": true
  }
}
